# Covara One — Runtime dependencies for Docker backend image
# Keep this file lean: only packages required to run the API in containers.

# Backend API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic[email]>=2.5.0
slowapi>=0.1.9

# Supabase / environment
supabase>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Caching
redis>=4.6.0,<5.0.0
fastapi-cache2[redis]>=0.2.1
jinja2>=3.1.0

# Runtime analytics and fraud logic
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0

# Evidence / HTTP / integrations
Pillow>=10.0.0
piexif>=1.1.3
httpx>=0.25.0
google-generativeai>=0.3.0
twilio>=9.0.0
aiokafka>=0.10.0
